{
  "name": "automation-initial-start-on-linux",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "build-all": "bun build ./index.ts --compile --outfile ./bin/install-all"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}

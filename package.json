{
  "name": "automation-initial-start-on-linux",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build-exec": "bun build ./install.ts --compile --outfile ./bin/install"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}